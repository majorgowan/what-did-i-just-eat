<div>
    <h4>
        Search Results:
    </h4>
    <div>
        <table>
            <% for (item of lookup) { %>
                <tr>
                    <td><%= item.fdcId %></td>
                    <td><%= item.description %></td>
                </tr>
            <% } %>
        </table>
    </div>
</div>